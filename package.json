{
  "name": "orbit-apps",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix connected-backend",
    "dev:editor": "npm run dev --prefix editor-web-app",
    "dev:dashboard": "npm run dev --prefix admin-panel",
    "dev:ws": "npm run dev --prefix connected-backend/mini-services/orbit-ws"
  }
}
